# logger.rb

class Logger
  def info(message)
    # Log the informational message
  end

  def warning(message)
    # Log the warning message
  end

  def error(message)
    # Log the error message
  end
end
